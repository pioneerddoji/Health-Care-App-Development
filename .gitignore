node_modules/
.expo/
# 빌드 산출물 (gitignore 는 인라인 주석을 지원하지 않는다 — 줄을 나눠 쓸 것)
# dist/     : npm run build:web — Cloudflare Pages 가 굽는다
# dist-web/ : 로컬 확인용 수동 export
dist/
dist-web/
*.log
.env
scripts/output/

# 임시 드래그 테스트 스크린샷
fb2-*.png
