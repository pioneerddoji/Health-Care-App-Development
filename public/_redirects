# Cloudflare Pages 리다이렉트 규칙 — 빌드 출력 루트로 그대로 복사된다.
#
# 이 앱은 React Navigation(파일 기반 라우터 아님)이라 실제 진입점은 index.html
# 하나뿐이다. 그래도 SPA 폴백을 두는 이유:
#   - 소셜 로그인 팝업이 돌아올 때 공급자가 경로를 덧붙이는 경우가 있다
#   - 사용자가 주소를 잘못 입력했을 때 404 대신 앱이 뜨는 편이 낫다
# 200 은 "리다이렉트하지 말고 이 파일을 그대로 응답하라"는 뜻이다.
/*    /index.html   200
