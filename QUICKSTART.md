# 혼자 먼저 써보기 (5분 가이드)

서버 설정 없이 휴대폰에서 바로 실사용할 수 있습니다. 데모(로컬) 모드도 이제
**기기에 저장**되어 앱을 껐다 켜도 기록·로그인 상태가 유지됩니다.

## 준비물
- 이 저장소를 받은 컴퓨터 (Node 20+)
- 휴대폰에 **Expo Go** 앱 설치 (App Store / Play스토어에서 "Expo Go" 검색)
- 컴퓨터와 휴대폰이 **같은 Wi-Fi**에 연결

## 실행 (3단계)

```bash
npm install     # 최초 1회
npm start
```

터미널에 QR 코드가 뜨면 —
- **iPhone**: 카메라 앱으로 QR 스캔 → Expo Go로 열기
- **Android**: Expo Go 앱을 열고 "Scan QR code"

## 첫 사용 흐름
1. **회원가입** (아무 이메일/비밀번호 8자↑ — 로컬 모드라 실제 발송 없음) → 동의 → 홈
2. 샘플 아이 2명(하은/도윤)이 보입니다. 내 아이만 쓰려면:
   홈에서 샘플 아이 선택 → ⚙️ 설정 → 데이터 관리 → 전체 삭제 (2명 각각)
3. **+ 아이 추가하기**로 내 아이 등록 → 기록 탭에서 오늘부터 기록 시작
4. 기록이 며칠 쌓이면 대시보드 그래프, 레포트 탭에서 PDF 생성·공유를 확인해 보세요

## 알아둘 것
- 데이터는 **이 휴대폰 안에만** 저장됩니다 (서버 전송 없음). 앱 재시작에도 유지되며,
  Expo Go에서 이 프로젝트 데이터를 지우거나 휴대폰을 바꾸면 사라집니다.
- 여러 기기 동기화·보호자 공동 관리·실제 공유 링크는 Supabase 연동(README의
  "Supabase 연동" 섹션, 약 30분) 후 동작합니다. 혼자 써보는 단계에서는 필요 없습니다.
- 데모 한계: 공유 링크는 가짜 URL(실제로 안 열림), 플랜 전환은 결제 없이 시뮬레이션.
- 컴퓨터를 꺼도 앱의 기존 데이터는 유지되지만, **새로 실행하려면** `npm start`가
  다시 필요합니다 (Expo Go는 개발 서버에서 JS를 받아옴). 컴퓨터 없이 쓰려면
  APK 빌드(`docs/06_deployment.md` B-3)로 넘어가세요.

## 문제 해결
- QR 스캔 후 연결 안 됨 → 같은 Wi-Fi인지 확인, 또는 `npm start` 후 터미널에서
  `s` 눌러 Expo Go 모드 확인, 방화벽이 있으면 `npx expo start --tunnel`
- 화면이 하얗게 나옴 → 터미널에서 `r` (리로드)
